# Workspace Rules

## Destructive Operations Safety
Never run a destructive operation (reset, delete, bulk overwrite) against the real primary account's data as part of a verification or test step without stopping and asking me first. Use a disposable test account for anything destructive going forward.
