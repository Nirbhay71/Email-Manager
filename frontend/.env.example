# Backend API origin. Defaults to http://localhost:5000 for local dev — set
# this to the real deployed backend URL once frontend and backend are hosted
# on different origins.
VITE_API_URL=http://localhost:5000
